machine:
  services:
    - docker
    
dependencies:
  override:
    - docker info
    - docker build --rm=false -t $REGISTRY_HOST/circleci .

test:
  override:
    - docker run -d hello-world  

deployment:
  hub:
    branch: master
    commands:
      - docker login $REGISTRY_URL  -u $DOCKER_USER -p $DOCKER_PASS 
      - docker push $REGISTRY_URL/circleci
